# 变更：将弹窗改为全页面体验

## Why
当前用户名生成器以小弹窗打开，空间受限。全页面布局可以提供更大的操作和展示区域，并可直接在浏览器标签页中使用。

## What Changes
- 增加专用的 HTML 页面承载用户名生成器 UI，替代弹窗。
- 更新扩展动作，使其触发时打开全页面视图（新标签页）。
- 调整样式与脚本以适配全页面上下文，保持现有的生成器行为（选项、长度同步、复制）。

## Impact
- 影响的规格：username-generator
- 影响的代码：manifest.json、popup.html/css/js（或新页面）、测试、打包命令
