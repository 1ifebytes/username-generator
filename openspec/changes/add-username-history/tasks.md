## 1. 存储与数据结构
- [x] 1.1 设计并实现存储结构（用户名 + 时间戳秒级），使用 `chrome.storage.local`
- [x] 1.2 生成流程（含初始自动生成）写入存储；遇到重复则更新时间并置顶
- [x] 1.3 保留最近 50 条，超出时移除最旧记录

## 2. UI 展示
- [x] 2.1 在全页面视图添加历史列表（生成器下方，限定高度可滚动），按时间倒序展示 YYYY-MM-DD HH:mm:ss（本地时区）
- [x] 2.2 支持懒加载（分批加载更多）
- [x] 2.3 支持点击历史项复制、单条删除与清空全部；空状态友好展示
- [x] 2.4 确保新记录生成后列表即时更新

## 3. 测试与验证
- [x] 3.1 增加 Jest 测试覆盖存储写入/读取、去重置顶、裁剪至 50 条、时间格式
- [ ] 3.2 手动验证：生成多条记录，检查倒序、时间格式、本地时区、懒加载、复制、删除、清空、刷新后仍可见
